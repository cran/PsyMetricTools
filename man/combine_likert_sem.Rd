% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/combine_likert_sem.R
\name{combine_likert_sem}
\alias{combine_likert_sem}
\title{Combine Likert Plot with SEM Path Diagram}
\usage{
combine_likert_sem(
  plot_likert,
  fit_sem,
  sem_args = list(),
  nodeLabels = NULL,
  ncol = 2,
  widths = rep(1, ncol),
  tag_levels = "A",
  tag_suffix = "",
  tag_pos = c(0.05, 0.95),
  tag_offset_y = 0.02,
  tag_size = 16,
  tag_face = "bold"
)
}
\arguments{
\item{plot_likert}{A ggplot object with Likert plot.}

\item{fit_sem}{A lavaan model object for semPaths.}

\item{sem_args}{List of additional arguments for semPaths.}

\item{nodeLabels}{Custom node labels for the SEM diagram.}

\item{ncol}{Number of columns in layout (default: 2).}

\item{widths}{Relative widths of columns.}

\item{tag_levels}{Tag levels for plot annotation (default: "A").}

\item{tag_suffix}{Suffix for tags.}

\item{tag_pos}{Position of tags as c(x, y).}

\item{tag_offset_y}{Vertical offset for tags.}

\item{tag_size}{Font size for tags.}

\item{tag_face}{Font face for tags.}
}
\value{
A combined patchwork plot.
}
\description{
Creates a combined visualization with a Likert plot and SEM path diagram.
}
\examples{
\donttest{
library(lavaan)
library(ggplot2)

# Create sample data
set.seed(123)
n <- 300
# Likert items (1-5) generated from 2 correlated latent factors, 3 items each
eta <- matrix(rnorm(n * 2), n, 2) \%*\% chol(0.7 * diag(2) + 0.3)
data <- as.data.frame(sapply(1:6, function(j) as.numeric(cut(
  0.7 * eta[, ceiling(j / 3)] + rnorm(n, 0, 0.7),
  c(-Inf, -1.5, -0.5, 0.5, 1.5, Inf)))))
names(data) <- paste0("Item", 1:6)

# Create Likert plot
likert_plot <- Plot_Likert(
  Data = data,
  name_items = "Item",
  ranges = 1:6
)

# Fit CFA model
model <- "
  F1 =~ Item1 + Item2 + Item3
  F2 =~ Item4 + Item5 + Item6
"
fit <- cfa(model, data = data, ordered = TRUE, estimator = "WLSMV")

# Combine Likert plot with SEM path diagram
combined_plot <- combine_likert_sem(
  plot_likert = likert_plot,
  fit_sem = fit,
  ncol = 2,
  widths = c(1, 1),
  tag_levels = "A",
  tag_size = 14
)

# Display combined plot
print(combined_plot)

# Save combined plot
ggsave(file.path(tempdir(), "combined_likert_sem.jpg"), combined_plot,
       width = 14, height = 8, dpi = 300)
}
}
