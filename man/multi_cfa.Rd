% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/multi_cfa.R
\name{multi_cfa}
\alias{multi_cfa}
\title{Run Multiple CFA Models}
\usage{
multi_cfa(modelos, data, estimator, ordered = TRUE, orthogonal_indices = NULL)
}
\arguments{
\item{modelos}{List of lavaan model syntax strings.}

\item{data}{Data frame with item responses.}

\item{estimator}{Estimation method (e.g., "WLSMV").}

\item{ordered}{Logical, whether variables are ordered/categorical (default: TRUE).}

\item{orthogonal_indices}{Optional vector of model indices to fit with orthogonal factors.}
}
\value{
A list with fits, fit indices, modification indices, correlations, and reliability.
}
\description{
Fits multiple CFA models and extracts fit indices, modification indices, and reliability.
}
\examples{
\donttest{
# Create sample data
set.seed(123)
n <- 300
# Likert items (1-5) generated from 3 correlated latent factors, 3 items each
eta <- matrix(rnorm(n * 3), n, 3) \%*\% chol(0.7 * diag(3) + 0.3)
data <- as.data.frame(sapply(1:9, function(j) as.numeric(cut(
  0.7 * eta[, ceiling(j / 3)] + rnorm(n, 0, 0.7),
  c(-Inf, -1.5, -0.5, 0.5, 1.5, Inf)))))
names(data) <- paste0("Item", 1:9)

# Define multiple CFA models to compare
model1 <- "F1 =~ Item1 + Item2 + Item3 + Item4 + Item5 + Item6 + Item7 + Item8 + Item9"

model2 <- "
  F1 =~ Item1 + Item2 + Item3
  F2 =~ Item4 + Item5 + Item6
  F3 =~ Item7 + Item8 + Item9
"

model3 <- "
  F1 =~ Item1 + Item2 + Item3 + Item4 + Item5
  F2 =~ Item6 + Item7 + Item8 + Item9
"

modelos <- list(model1, model2, model3)

# Run multiple CFAs
results <- multi_cfa(
  modelos = modelos,
  data = data,
  estimator = "WLSMV",
  ordered = TRUE
)

# Compare fit indices across models
results$bondades_ajuste

# Check modification indices for Model 2
results$indices_modificacion[[2]]

# View factor correlations for Model 2
results$correlaciones_factores$Modelo2

# Check reliability (omega) for each model
results$fiabilidad

# Fit model 3 with orthogonal factors
results_orth <- multi_cfa(
  modelos = modelos,
  data = data,
  estimator = "WLSMV",
  ordered = TRUE,
  orthogonal_indices = 3
)
}
}
