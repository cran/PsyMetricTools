% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/safe_cfa.R
\name{safe_measures}
\alias{safe_measures}
\title{Safe Fit Measures Extraction}
\usage{
safe_measures(
  fit,
  wanted = c("chisq.scaled", "df.scaled", "cfi.scaled", "tli.scaled", "rmsea.scaled",
    "srmr", "wrmr")
)
}
\arguments{
\item{fit}{A fitted lavaan object or \code{NULL}.}

\item{wanted}{Character vector of fit measure names to extract (default
includes chi-square, df, CFI, TLI, RMSEA, SRMR, and WRMR, all scaled).}
}
\value{
A one-row data frame with the requested fit measures.
}
\description{
Extracts selected fit measures from a lavaan object. Returns a
  one-row data frame with \code{NA} values for any measures that could not be
  retrieved (e.g., when the model is \code{NULL}).
}
\examples{
\donttest{
set.seed(123)
# Likert items (1-5) generated from one latent factor, 3 items each
eta <- matrix(rnorm(200 * 1), 200, 1) \%*\% chol(0.7 * diag(1) + 0.3)
mydata <- as.data.frame(sapply(1:3, function(j) as.numeric(cut(
  0.7 * eta[, ceiling(j / 3)] + rnorm(200, 0, 0.7),
  c(-Inf, -1.5, -0.5, 0.5, 1.5, Inf)))))
names(mydata) <- paste0("x", 1:3)
model <- 'F1 =~ x1 + x2 + x3'
fit <- safe_cfa(model, data = mydata, items = c("x1", "x2", "x3"))
safe_measures(fit)
}
}
