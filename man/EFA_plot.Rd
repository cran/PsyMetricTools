% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/EFA_plot.R
\name{EFA_plot}
\alias{EFA_plot}
\title{Plot EFA Factor Loadings}
\usage{
EFA_plot(specifications, item_prefix)
}
\arguments{
\item{specifications}{Lavaan model object or a data.frame with columns:
est.std, ci.lower, ci.upper, lhs, rhs, op.}

\item{item_prefix}{Prefix for item names.}
}
\value{
A ggplot object showing factor loadings.
}
\description{
Creates a visualization of factor loadings from EFA.
}
\examples{
\donttest{
# Example 1: Using with EFA_modern() result
set.seed(123)
n <- 300
# Likert items (1-5) generated from 3 correlated latent factors, 3 items each
eta <- matrix(rnorm(n * 3), n, 3) \%*\% chol(0.7 * diag(3) + 0.3)
data_efa <- as.data.frame(sapply(1:9, function(j) as.numeric(cut(
  0.7 * eta[, ceiling(j / 3)] + rnorm(n, 0, 0.7),
  c(-Inf, -1.5, -0.5, 0.5, 1.5, Inf)))))
names(data_efa) <- paste0("Item", 1:9)

# First run EFA
efa_result <- EFA_modern(
  n_factors = 3,
  n_items = 9,
  name_items = "Item",
  data = data_efa,
  apply_threshold = TRUE
)

# Plot factor loadings using the lavaan specification
plot <- EFA_plot(
  specifications = efa_result$Specifications[[3]],
  item_prefix = "Item"
)
print(plot)

# Example 2: Using with a lavaan object directly
library(lavaan)
model <- "
  efa('efa')*f1 +
  efa('efa')*f2 +
  efa('efa')*f3 =~ Item1 + Item2 + Item3 + Item4 + Item5 +
                   Item6 + Item7 + Item8 + Item9
"
fit <- sem(model, data = data_efa, rotation = "oblimin")
EFA_plot(fit, item_prefix = "Item")
}
}
