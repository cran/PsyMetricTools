% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Standardized_solutions.R
\name{Standardized_solutions}
\alias{Standardized_solutions}
\title{Standardized Factor Solutions for EFA}
\usage{
Standardized_solutions(specification, name_items, apply_threshold = TRUE)
}
\arguments{
\item{specification}{A lavaan model object.}

\item{name_items}{Prefix for item names.}

\item{apply_threshold}{Logical, whether to apply 0.30 threshold (default TRUE).}
}
\value{
A data frame with items and factor loadings.
}
\description{
Extracts and formats standardized factor loadings from lavaan EFA.
}
\examples{
\donttest{
# First run EFA_modern to get the specification object
set.seed(123)
n <- 300
# Likert items (1-5) generated from 3 correlated latent factors, 3 items each
eta <- matrix(rnorm(n * 3), n, 3) \%*\% chol(0.7 * diag(3) + 0.3)
data_efa <- as.data.frame(sapply(1:9, function(j) as.numeric(cut(
  0.7 * eta[, ceiling(j / 3)] + rnorm(n, 0, 0.7),
  c(-Inf, -1.5, -0.5, 0.5, 1.5, Inf)))))
names(data_efa) <- paste0("Item", 1:9)

# Run EFA
efa_result <- EFA_modern(
  n_factors = 3,
  n_items = 9,
  name_items = "Item",
  data = data_efa,
  apply_threshold = TRUE
)

# Extract standardized solutions from the 3-factor model
loadings <- Standardized_solutions(
  specification = efa_result$Specifications[[3]],
  name_items = "Item",
  apply_threshold = TRUE
)
print(loadings)

# Without threshold (show all loadings)
loadings_full <- Standardized_solutions(
  specification = efa_result$Specifications[[3]],
  name_items = "Item",
  apply_threshold = FALSE
)
print(loadings_full)
}
}
