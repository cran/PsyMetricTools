% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/safe_cfa.R
\name{safe_cfa}
\alias{safe_cfa}
\title{Safe CFA Wrapper}
\usage{
safe_cfa(syntax, data, items, estimator = "WLSMV", stdlv = TRUE)
}
\arguments{
\item{syntax}{A character string with the lavaan model syntax.}

\item{data}{A data frame containing the observed variables.}

\item{items}{Character vector of item names to treat as ordered (categorical).}

\item{estimator}{Estimator to use (default \code{"WLSMV"}).}

\item{stdlv}{Logical; standardize latent variables (default \code{TRUE}).}
}
\value{
A fitted lavaan object, or \code{NULL} if the model failed.
}
\description{
Fits a CFA model using \code{\link[lavaan]{cfa}} with error
  handling. Returns \code{NULL} instead of raising an error when the model
  fails to converge or encounters estimation problems.
}
\examples{
\donttest{
set.seed(123)
# Likert items (1-5) generated from one latent factor, 3 items each
eta <- matrix(rnorm(200 * 1), 200, 1) \%*\% chol(0.7 * diag(1) + 0.3)
mydata <- as.data.frame(sapply(1:3, function(j) as.numeric(cut(
  0.7 * eta[, ceiling(j / 3)] + rnorm(200, 0, 0.7),
  c(-Inf, -1.5, -0.5, 0.5, 1.5, Inf)))))
names(mydata) <- paste0("x", 1:3)
model <- 'F1 =~ x1 + x2 + x3'
fit <- safe_cfa(model, data = mydata, items = c("x1", "x2", "x3"))
if (!is.null(fit)) summary(fit)
}
}
