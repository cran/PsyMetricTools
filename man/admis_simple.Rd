% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/safe_cfa.R
\name{admis_simple}
\alias{admis_simple}
\title{Simple Admissibility Check for CFA}
\usage{
admis_simple(fit, latents)
}
\arguments{
\item{fit}{A fitted lavaan object or \code{NULL}.}

\item{latents}{Character vector of latent variable names to check.}
}
\value{
A character string: \code{"OK"} if admissible,
  \code{"NONCONV/FAIL"} if the model is \code{NULL}, or a description of
  the problems found.
}
\description{
Checks whether a fitted CFA model has admissible solutions by
  detecting negative residual variances (Heywood cases) and standardized
  factor loadings with absolute value greater than 1.
}
\examples{
\donttest{
set.seed(123)
# Likert items (1-5) generated from one latent factor, 3 items each
eta <- matrix(rnorm(200 * 1), 200, 1) \%*\% chol(0.7 * diag(1) + 0.3)
mydata <- as.data.frame(sapply(1:3, function(j) as.numeric(cut(
  0.7 * eta[, ceiling(j / 3)] + rnorm(200, 0, 0.7),
  c(-Inf, -1.5, -0.5, 0.5, 1.5, Inf)))))
names(mydata) <- paste0("x", 1:3)
model <- 'F1 =~ x1 + x2 + x3'
fit <- safe_cfa(model, data = mydata, items = c("x1", "x2", "x3"))
latents <- lavaan::lavNames(fit, type = "lv")
admis_simple(fit, latents)
}
}
