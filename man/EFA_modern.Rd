% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/EFA_modern.R
\name{EFA_modern}
\alias{EFA_modern}
\title{Modern Exploratory Factor Analysis}
\usage{
EFA_modern(
  n_factors,
  n_items,
  name_items,
  data,
  apply_threshold,
  estimator = "WLSMV",
  rotation = "oblimin",
  exclude_items = NULL
)
}
\arguments{
\item{n_factors}{Number of factors to extract.}

\item{n_items}{Number of items.}

\item{name_items}{Prefix for item names.}

\item{data}{Data frame containing the items.}

\item{apply_threshold}{Logical, whether to apply threshold to factor loadings.}

\item{estimator}{Estimator to use (default "WLSMV").}

\item{rotation}{Rotation method (default "oblimin").}

\item{exclude_items}{Items to exclude (default NULL).}
}
\value{
A list containing fit indices, specifications, interfactor correlations, and pattern matrix.
}
\description{
Performs exploratory factor analysis using lavaan with rotation.
}
\examples{
\donttest{
# Create sample data with 15 Likert-type items
set.seed(123)
n <- 300
# Likert items (1-5) generated from 3 correlated latent factors, 5 items each
eta <- matrix(rnorm(n * 3), n, 3) \%*\% chol(0.7 * diag(3) + 0.3)
data_efa <- as.data.frame(sapply(1:15, function(j) as.numeric(cut(
  0.7 * eta[, ceiling(j / 5)] + rnorm(n, 0, 0.7),
  c(-Inf, -1.5, -0.5, 0.5, 1.5, Inf)))))
names(data_efa) <- paste0("Item", 1:15)

# Perform EFA with 3 factors using WLSMV estimator
result <- EFA_modern(
  n_factors = 3,
  n_items = 15,
  name_items = "Item",
  data = data_efa,
  apply_threshold = TRUE,
  estimator = "WLSMV",
  rotation = "oblimin"
)

# Access fit indices
result$Bondades_Original

# Access pattern matrix (factor loadings)
result$result_df

# Access interfactor correlations
result$InterFactor

# Example excluding specific items
result2 <- EFA_modern(
  n_factors = 3,
  n_items = 15,
  name_items = "Item",
  data = data_efa,
  apply_threshold = TRUE,
  estimator = "WLSMV",
  rotation = "oblimin",
  exclude_items = c("Item5", "Item10")
)
}
}
